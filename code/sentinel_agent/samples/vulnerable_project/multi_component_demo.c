#include <png.h>
#include <zlib.h>
#include <curl/curl.h>
#include <sqlite3.h>
#include <libxml/parser.h>

void dependency_probe_demo() {
    /*
     * This file is only used by SENTINEL Agent A dependency parsing tests.
     * It is not intended to compile in the current minimal sample project.
     */
}
